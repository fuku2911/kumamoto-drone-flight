# 開発記録

## 2026-09-24 — Version 0.1の状態整理

### 今回行ったこと
- ユーザー確認済みのStep 8をac51f5c（feat: improve beginner course readability and playability）としてコミット。直後のgit statusは変更なし。
- READMEをVersion 0.1（package.jsonは0.1.0）の説明に整理し、実装済み機能、操作、確認方法、未実装項目、既知の制約を明記。
- ソースのキー割り当て、ゲーム状態、タイマー、カメラ設定とREADMEの一致を確認。
- 新しいゲーム機能は追加していない。リモート未設定を確認し、GitHubリポジトリ作成・Pushは未実施。

### 現在正常に動くこと
- Step 8までユーザーによる実プレイ確認済み。START、5リング順次通過、FINISH、RETRY、再プレイが可能。
- npm.cmd run buildの最終確認が成功。JavaScriptは540.35 kB、gzip 135.29 kB。
- git diff --check、差分、コミット履歴、git statusを確認。

### 未解決問題
- 既知の500 kB超のビルドサイズ警告は継続。動作エラーは報告されていない。
- 今回整理したREADME.mdと本ログは未コミット。ゲーム本体はStep 8のコミットに保存済み。
- 公開URLはまだない。Version 0.1タグも未作成。

### 次に行うこと
- 文書整理のコミット候補：docs: document version 0.1 release state。
- 承認後にGitHubで空リポジトリを作成し、認証・origin設定・初回Pushを行う。その後に静的サイト公開を検討する。

## 2026-09-24 — Step 8：Version 0.1の遊びやすさ調整

### 今回行ったこと
- ユーザーによる通しプレイ確認と既存のブラウザ検証結果に加え、Step 7のコード・差分・ビルドを最終確認。
- Step 7を6a190b5（feat: add game loop timer finish and retry）としてコミット。直後のgit statusは変更なし。
- コースを緩やかな右カーブに変更。前後間隔を17から26、高さ変化を各区間1単位にした。
- リング半径を6から7、中心との通過判定距離を2から3へ拡大。
- 対象は明るい黄色、未通過は不透明度40%の青、通過済みは22%の緑。通過後の手前の輪が視界を塞ぎにくくした。
- コースを覆うよう地面を180四方に拡張。操作方式・移動速度・カメラ設定は変更していない。

### 現在正常に動くこと
- 通常ページでSTART直後の最初の黄色いリングを視覚確認。
- 一時検証ページで既存のキー入力イベントから5個すべて通過。各通過後の対象色・次リング中心が画角内にあることを確認し、各区間をスクリーンショットで確認。
- FINISHとタイマー停止、RETRYによる位置・リング・タイマー初期化、再スタート後の1番通過を確認。
- 検証中のコンソール警告・エラーなし。一時検証ファイルは削除。
- npm.cmd run build成功。git diffとgit diff --checkを確認。

### 未解決問題
- 遠方の未通過リング同士には一部投影上の重なりが残るが、対象を強調して区別できる状態。遊びやすさの最終評価はユーザーの実操作確認を待つ。
- 既知の500 kB超のビルド警告は継続。
- Step 8は未コミット、Push未実施。

### 次に行うこと
- ユーザーの操作確認を待ち、必要なら今回の範囲で微調整する。

## 2026-09-23 — Step 7：開始・タイマー・完走・リトライ

### 今回行ったこと
- Step 6を471a561（feat: add sequential checkpoint rings）としてコミット。直後のgit statusは変更なし。
- READY・PLAYING・FINISHEDを管理し、PLAYING中のみ入力・移動・リング判定を有効化。
- 開始画面にタイトル・START・操作方法、左上にTIMEと対象番号、完走画面にFINISH・最終タイム・RETRYを追加。
- performance.now()の差で時間を計測し、5個目の通過時に固定。別タブにいる時間も含める。
- RETRYで機体の位置・向き、カメラ、リング、キー状態、フレーム時刻、タイマーを初期化してREADYへ戻す。再読み込み不要。

### 現在正常に動くこと
- ブラウザの一時検証ページで7組の検証に成功。検証用ファイルは確認後削除。
- 起動時のSTART表示、READY中の操作不可、START後の計測開始、時刻書式と分繰り上がりを確認。
- キー入力イベントで機体基準の旋回・移動を行い、リング5個の順次通過とHUD更新を確認。
- FINISH画面、時間と操作の停止、RETRYの全状態リセット、2回連続完走を確認。
- 通常ページで実際のSTARTクリックとHUD表示を確認。開始画面・完走画面のレイアウトを視覚確認。
- コンソール警告・エラーなし。npm.cmd run build成功。git diffとgit diff --check確認済み。

### 未解決問題
- 確認範囲で動作上の問題なし。既知の500 kB超のビルド警告は継続（gzip約135 kB）。
- Step 7は未コミット、Push未実施。

### 次に行うこと
- ユーザーの通しプレイ確認を待つ。次の作業は承認後に進める。

## 2026-09-23 — Step 6：チェックポイントリング

### 今回行ったこと
- Step 5を8984259（feat: add smooth third-person follow camera）としてコミット。直後のgit statusは変更なし。
- TorusGeometryで5個のリングを作成し、内部順序1〜5を設定。中心高さ7・8・10・9・11で前方へ配置。
- 現在対象の中心から機体中心までの距離が2以下なら次へ進む。対象のみ黄色に発光、未通過は青、通過済みは緑。
- 全通過時は完了ログを1回出す。タイマー・ゲームクリア画面等は追加していない。
- コースを覆うよう地面と格子を120四方に広げ、中心をZ=-35へ移動。

### 現在正常に動くこと
- ブラウザの一時検証ページで11項目成功。確認後に検証ページを削除。
- 5個の配置・順序・地面からの浮上・初期対象色、順番飛ばしの拒否、距離範囲外の拒否を確認。
- キー入力イベントから既存の移動処理を実行し、5個を順番に通過。各段階の対象・通過済み色の切り替えを確認。
- 完了ログは1回、完了後の対象なし、キー解放で停止、Q/E旋回と機体基準移動、最低高度、カメラ追従を確認。
- 通常ページでも5個のリングと黄色の対象を視覚確認。コンソール警告・エラーなし。
- npm.cmd run build成功。git diffとgit diff --check確認済み。

### 未解決問題
- 確認範囲で動作上の問題なし。通過は中心付近への接近判定で、通過方向は限定しない。
- 既知の500 kB超のビルド警告は継続（gzip約134.5 kB）。
- Step 6は未コミット、Push未実施。

### 次に行うこと
- ユーザーの操作確認を待ち、次の機能は仕様・範囲の承認後に進む。

## 2026-09-23 — Step 5：追従カメラ

### 今回行ったこと
- Step 4を7845960（feat: add keyboard drone controls）としてコミット。直後のgit statusは変更なし。
- 機体基準の後方18・上方7へ位置を補間し、機体の2単位前方を見る三人称カメラを追加。
- 補間係数は1-exp(-8×deltaTime)。初期表示は目標位置に即時配置。
- 入力処理と機体の移動・旋回処理は変更なし。依存追加なし。

### 現在正常に動くこと
- ブラウザの一時検証ページで24項目成功。検証ページは確認後に削除。
- 横長16:9・縦長2:3で前後左右・上下・Q/E旋回・移動と旋回の同時入力に追従し、各フレームで機体全体が画角内にあることを確認。
- 停止後は後方の目標位置に収束。静止時の位置の揺れなし。30/60/144fps相当で静止目標への補間結果が一致。
- 機体基準の前進、キー解放による停止、実際の描画ループでの追従を確認。
- 通常ページで停止時の見やすさを視覚確認。コンソール警告・エラーなし。
- npm.cmd run build成功。git diffとgit diff --checkを確認。

### 未解決問題
- 初期案では縦長画面の旋回時に機体端が画角を外れる可能性があったため、後方距離を14から18へ調整して再検証済み。
- 既知の500 kB超のビルド警告は継続（gzip約134 kB）。
- Step 5は未コミット、Push未実施。

### 次に行うこと
- ユーザーの操作確認を待つ。次の機能は仕様・範囲を決めてから実装する。

## 2026-09-23 — Step 4：キーボード操作

### 今回行ったこと
- Step 3を892feb7（feat: add placeholder quadcopter model）としてコミット。直後のgit statusは変更なし。
- keydown/keyupで状態を管理し、W/S/A/Dの機体基準移動、Space/左右Shiftの上下移動、Q/EのY軸旋回を追加。
- MOVE_SPEED=6、TURN_SPEED=90度/秒、MIN_ALTITUDE=0.5を定数化。複数方向の移動は正規化し合計速度を一定にした。
- requestAnimationFrameの時刻からdeltaTimeを計算。長いフレームは0.05秒で制限し、フォーカス喪失・非表示時は入力を解除。
- カメラは固定。依存パッケージ追加なし。

### 現在正常に動くこと
- 一時検証ページで実装ソースを読み込み、ブラウザのKeyboardEventを入力ハンドラへ送信して18項目を検証し、すべて成功。検証ページは確認後に削除。
- W/S、A/D、Space/左右Shift、Q/E、左右旋回後のW/S/A/D、キー解放による停止、W＋Space、最低高度、逆方向キーの相殺を確認。
- 30/60/144fps相当で1秒間の移動量・旋回量が一致。
- 実際の描画ループでもW＋Space、解放後の停止、Qの旋回を確認。フォーカス喪失時の入力解除、固定カメラも確認。
- 通常ページのブラウザ表示とキー入力を確認し、コンソール警告・エラーなし。
- npm.cmd run build成功。git diffとgit diff --checkを確認。

### 未解決問題
- 動作上の問題は確認範囲ではなし。固定カメラなので遠くへ移動すると画面外へ出る。再読み込みで初期位置へ戻る。
- 既知の500 kB超のビルド警告あり。JavaScriptは537.16 kB（gzip 133.98 kB）。
- Step 4は指示どおり未コミット、Push未実施。

### 次に行うこと
- ユーザーの操作確認を待ち、承認後にStep 5の追従カメラへ進む。

## 2026-09-23 — Step 3：仮ドローンの表示

### 今回行ったこと
- Step 2をcba0b52（feat: add minimal Three.js scene）としてコミットし、直後のgit statusが変更なしであることを確認。
- createDrone関数で中央機体、前後左右の4本のアーム、4個のローターを生成。全体をTHREE.Groupにまとめた。
- 前方向を-Zとし、オレンジ色の先端・前アーム・前ローターで示した。
- 機体中心を(0, 3, 0)に配置。外部素材や依存パッケージは追加していない。
- 操作、飛行、旋回、物理演算、プロペラアニメーション、追従カメラ、リング、タイマー、UIは未実装。

### 現在正常に動くこと
- Chromeで機体、4個のローター、オレンジ色の前方向を視覚確認。
- 地面に埋まっていないことを確認。中央機体の底面は地面から2.675単位上にある。
- ブラウザの警告・エラーログなし。
- npm.cmd run buildが成功。git diffとgit diff --checkを確認。

### 未解決問題
- 動作上の問題は確認範囲ではなし。
- 前回同様、ビルド時に500 kB超のチャンク警告あり。JavaScriptは536.17 kB（gzip 133.62 kB）。
- Step 3の変更はユーザーの指示により未コミット。

### 次に行うこと
- ユーザーの表示確認を待ち、承認後にStep 4の操作処理へ進む。

## 2026-09-23 — コミット用メールアドレスの変更

### 今回行ったこと
- このリポジトリのuser.emailをGitHubのnoreplyアドレスへ変更。
- 未Pushの初回コミットのAuthor・Committerを同じアドレスに変更。コミットIDは081bda6からeabc216へ変更された。

### 現在正常に動くこと
- 初回コミットのファイル内容は維持し、Step 2の5ファイルの変更も未コミットのまま保持。
- git statusとコミットの作成者情報を確認。Pushは未実施。

### 未解決問題
- なし。

### 次に行うこと
- Step 2のユーザー確認を待つ。

## 2026-09-23 — Step 2：最小3D空間

### 今回行ったこと
- 作成者名・メールアドレスをこのリポジトリ内だけに設定。
- Step 1を081bda6（chore: initialize Vite and Three.js project）としてコミットし、直後のgit statusが変更なしであることを確認。
- Scene、PerspectiveCamera、WebGLRenderer、地面と格子、空色の背景、AmbientLight、DirectionalLightを追加。
- 固定カメラを斜め上に配置し、リサイズ対応とrequestAnimationFrameの描画ループを実装。
- ドローン、操作、リング、タイマー、ゲームUIは追加していない。

### 現在正常に動くこと
- npm.cmd run buildが成功。
- 開発サーバーをChromeで開き、地面と奥行き、背景を視覚確認。
- 横長表示から600×900の縦長へ変更しても正常表示し、キャンバスの表示サイズと描画バッファが600×900に追従することを確認。
- ブラウザの警告・エラーログなし。

### 未解決問題
- 動作上の問題は確認範囲ではなし。
- ビルド時に500 kB超のチャンク警告あり。JavaScriptは532.37 kB（gzip 132.37 kB）。現段階では分割や警告抑制を追加していない。
- Step 2の変更はユーザーの指示により未コミット。

### 次に行うこと
- ユーザー確認後、承認された段階でStep 3の仮ドローン表示へ進む。

## 2026-09-23 — Step 1：開発環境

### 今回行ったこと
- mainブランチでGitを初期化し、.gitignoreを作成。
- AGENTS.md、README.mdと最小HTML・JavaScript・CSSを作成。
- Three.js 0.186.0、Vite 8.3.0を導入し、package-lock.jsonを生成。
- 3D空間、ドローン、操作処理は実装していない。

### 現在正常に動くこと
- npm.cmd run dev -- --host 127.0.0.1で開発サーバーが起動。
- ブラウザでタイトル・Step 1の案内・説明文を表示。警告・エラーログなし。
- npm.cmd run buildが成功。
- npm.cmd run preview -- --host 127.0.0.1でも同じ表示を確認。警告・エラーログなし。
- npmインストール時の監査は脆弱性0件。

### 未解決問題
- Step 1の確認範囲ではなし。
- 最初のnpm取得は実行環境のアクセス制限（EACCES）で失敗したが、許可された再実行で導入完了。
- 初回コミットは未実施。作業終了時に推奨メッセージを提示する。

### 次に行うこと
- ユーザーの承認後、Step 2で地面・光・カメラによる3D空間を表示する。
