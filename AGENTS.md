# Kumamoto Drone Flight 開発ルール

## 方針と範囲
- 最小限の遊べるMVPを早く公開し、プレイヤーの反応を見て改善する。
- 承認されたStepだけを実装し、次の機能へ勝手に進まない。
- 現在はStep 4（キーボードによる移動・旋回）まで。機体の傾き、慣性、重力、衝突判定、プロペラアニメーション、追従カメラ、リング、タイマー、ゲームUIは未実装。
- 技術はVite、Vanilla JavaScript、Three.js、HTML、CSSを基本とする。
- Version 0.1ではReact、Next.js、TypeScript、データベース、登録・ログイン、外部AI API、Jev、マルチプレイヤーを使わない。
- スマートフォン操作、ゲームパッド、ランキング、課金、広告等の将来機能を先行実装しない。

## 実装
- 不要なライブラリを追加せず、過剰にファイルを分割しない。
- 初期化、入力、移動、カメラ、描画は必要になった段階で関数に分け、後から分割しやすくする。
- タイトルの変更箇所はまとめる。
- 操作体系の変更や大きな設計変更は実装前に説明する。
- Windows環境を前提とする。PowerShellではnpm.cmdを使用する。
- 静的サイトとしてビルドできる構造を維持する。
- 素材は必要になってからpublic/models、public/textures、public/sounds、public/images等へ配置する。空フォルダを大量に作らない。
- エラーを解消してから次の機能へ進む。

## Gitと確認
- 大きな変更前にgit statusと既存ファイルを確認する。
- ファイルの大量削除・大量移動を勝手に行わない。
- 実装後は動作、エラー、ビルド、git status、git diff、新規ファイルの内容を確認する。
- node_modulesとdistは追跡せず、package-lock.jsonは追跡する。
- 作業の区切りごとに変更内容と推奨コミットメッセージを提示する。コミットはユーザーから指示された場合に行う。
- GitHubへ勝手にPushしない。

## 記録
- README.mdには起動方法、ビルド方法、現在の実装範囲を記載する。
- DEVELOPMENT_LOG.mdには各作業終了時に「今回行ったこと」「現在正常に動くこと」「未解決問題」「次に行うこと」を記録する。
- docs/、SPEC.md、ROADMAP.mdは必要になった段階で追加する。
